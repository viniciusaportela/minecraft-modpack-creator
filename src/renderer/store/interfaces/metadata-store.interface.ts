export interface IMetadata {
  modCount: number;
  modLoader: string;
  version: number;
  worldPath: string;
  timestamp: number;
  minecraftVersion: string;
  loaderVersion: string;
  path: string;
}
